[phases.setup]
nixPkgs = ["jdk21"]

[phases.build]
cmds = ["./gradlew bootJar --no-daemon -x test"]

[start]
cmd = "java $JAVA_OPTS -Dspring.profiles.active=${SPRING_PROFILES_ACTIVE:-railway} -Dserver.port=${PORT:-8080} -jar build/libs/app.jar"
